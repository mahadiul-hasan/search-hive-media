export enum tagTypes {
	user = "user",
	contact = "contact",
	searchFeed = "searchFeed",
	searchStat = "searchStat",
	click = "click",
	dashboard = "dashboard",
	redirect = "redirect",
	shortUrl = "shortUrl",
}

export const tagTypesList = [
	tagTypes.user,
	tagTypes.contact,
	tagTypes.searchFeed,
	tagTypes.searchStat,
	tagTypes.click,
	tagTypes.dashboard,
	tagTypes.redirect,
	tagTypes.shortUrl,
];
